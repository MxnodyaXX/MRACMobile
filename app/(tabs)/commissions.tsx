import { Percent } from 'lucide-react-native';

import { ComingSoon } from '@/src/components/ComingSoon';

export default function CommissionsScreen() {
  return <ComingSoon title="Commissions" icon={Percent} />;
}
